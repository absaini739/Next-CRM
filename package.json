{
    "name": "ispecia-crm",
    "private": true,
    "workspaces": [
        "apps/*"
    ],
    "scripts": {
        "predev": "bash -c 'echo \"🔍 Starting database containers...\" && podman start laravel-crm-db laravel-crm-redis 2>/dev/null || true && echo \"⏳ Waiting for database...\" && sleep 2 && echo \"✅ Database ready! Starting servers...\"'",
        "dev": "concurrently \"npm run dev -w apps/api\" \"npm run dev -w apps/web\"",
        "build": "npm run build --workspaces",
        "test": "npm run test --workspaces",
        "start-db": "./start-db.sh"
    },
    "devDependencies": {
        "concurrently": "^8.2.2"
    }
}